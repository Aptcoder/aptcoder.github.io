<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Samuel Omilo</title><link>https://samuelomilo.com/</link><description>Recent content on Samuel Omilo</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Sun, 09 Aug 2026 10:51:00 +0100</lastBuildDate><atom:link href="https://samuelomilo.com/index.xml" rel="self" type="application/rss+xml"/><item><title>I built a tiny taint analyser for Python</title><link>https://samuelomilo.com/posts/built-taint-analyser/</link><pubDate>Sun, 09 Aug 2026 10:51:00 +0100</pubDate><guid>https://samuelomilo.com/posts/built-taint-analyser/</guid><description>As part of my bachelor&amp;rsquo;s in computer science, I learnt about the risks of trusting user input without checking it first. It was a fact I knew, not a problem I&amp;rsquo;d actually felt.
That changed recently. I had to do some research into security approaches, especially the less commonly discussed ones, and while doing that I learnt about the importance of ensuring the data you process as input from users is safe for such processing and similarly that the data you display to users is safe for display.</description><content:encoded><![CDATA[<p>As part of my bachelor&rsquo;s in computer science, I learnt about the risks of trusting user input without checking it first. It was a fact I knew, not a problem I&rsquo;d actually felt.</p>
<p>That changed recently. I had to do some research into security approaches, especially the less commonly discussed ones, and while doing that I learnt about the importance of ensuring the data you process as input from users is safe for such processing and similarly that the data you display to users is safe for display. Ensuring this is typically achieved using a process of static (or dynamic) analysis called &ldquo;Taint analysis&rdquo;</p>
<p>For some more clarity, imagine a login form where the username field gets dropped straight into a SQL query without being escaped, so a crafted input can rewrite the query and dump the whole users table. Or imagine a user&rsquo;s bio field containing a script tag, and that field getting rendered straight into a page without being escaped first, so now that data is running in someone else&rsquo;s browser as if it were part of your app. Different sinks, same shape of problem: data comes from somewhere untrusted and ends up somewhere dangerous, without anyone checking what happened in between.</p>
<p>The two ends of that flow have names. The place data comes in from is called a <strong>source</strong>, and the place it ends up doing damage is called a <strong>sink</strong>. In the first example, the username field is the source and the SQL query is the sink; in the second, the bio field is the source and the HTML render is the sink.</p>
<p>That&rsquo;s the whole framing behind taint analysis: mark data as tainted at the sources, follow it through the program, and raise a flag if it reaches a sink without being cleaned up along the way.</p>
<p>In this blog post, I&rsquo;ll talk about building a taint analyser for a small subset of Python. It utilises Python&rsquo;s ast, so a basic idea of parsing and abstract syntax trees (AST) would be needed to follow this post.</p>
<h2 id="how-it-works">How it works</h2>
<p>The taint analyser built uses static analysis. It iterates through the syntax tree of a program and tracks sinks and sources.</p>
<h3 id="1-parsing-the-code-into-a-tree">1. Parsing the code into a tree</h3>
<p>To keep things simple, I used the Python ast library, which generates the semantic tree of a given Python code. Having the tree means the work is simplified to iterating the nodes in the tree to identify where sources are, note them, and flag whenever they&rsquo;re present at a sink. Here&rsquo;s what the tree generated looks like when printed:</p>
<h3 id="2-marking-sources-and-sinks">2. Marking sources and sinks</h3>
<p>Marking the source and sink calls comes down to recognising specific function calls by name as you walk the tree. A call node in the AST doesn&rsquo;t just hand you a clean string like &ldquo;input&rdquo; though, it can be a Name (for something like input(&hellip;)) or an Attribute (for something like os.system(&hellip;)), so I wrote a small helper, call_name, that normalises both shapes into one string:</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">call_name</span>(node):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> isinstance(node<span style="color:#f92672">.</span>func, ast<span style="color:#f92672">.</span>Name):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> node<span style="color:#f92672">.</span>func<span style="color:#f92672">.</span>id
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> isinstance(node<span style="color:#f92672">.</span>func, ast<span style="color:#f92672">.</span>Attribute):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> node<span style="color:#f92672">.</span>func<span style="color:#f92672">.</span>attr
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">None</span></span></span></code></pre>
</figure><p>With that in place, sources and sinks are just two sets of strings I check against:</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span>SOURCES <span style="color:#f92672">=</span> {<span style="color:#e6db74">&#34;input&#34;</span>}
</span></span><span style="display:flex;"><span>SINKS <span style="color:#f92672">=</span> {<span style="color:#e6db74">&#34;eval&#34;</span>, <span style="color:#e6db74">&#34;exec&#34;</span>, <span style="color:#e6db74">&#34;system&#34;</span>}</span></span></code></pre>
</figure><p>sys.argv gets handled separately since it&rsquo;s not a call, it&rsquo;s a subscript on an attribute (sys.argv[1]), so it needs its own structural check rather than a name lookup.</p>
<h3 id="3-the-tainted-set">3. The tainted set</h3>
<p>The whole analyser really comes down to one piece of state: a set of variable names that are currently tainted. Every time I visit an assignment, I decide whether the name being assigned to belongs in that set:</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">visit_Assign</span>(self, node):
</span></span><span style="display:flex;"><span>    target <span style="color:#f92672">=</span> node<span style="color:#f92672">.</span>targets[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>id
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> is_tainted_expr(node<span style="color:#f92672">.</span>value):
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>tainted<span style="color:#f92672">.</span>add(target)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>tainted<span style="color:#f92672">.</span>discard(target)</span></span></code></pre>
</figure><p>The discard matters as much as the add. If a variable was tainted and then gets overwritten with something clean, it needs to leave the set, otherwise, the analyser would keep flagging a variable long after it stopped being dangerous.</p>
<h3 id="4-is-this-expression-tainted">4. Is this expression tainted?</h3>
<p>This is the one function doing most of the actual thinking. Given any expression node, it needs to answer a single question: could this evaluate to something tainted?</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">is_tainted_expr</span>(node):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> isinstance(node, ast<span style="color:#f92672">.</span>Call):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> call_name(node) <span style="color:#f92672">in</span> SOURCES:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">True</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> any(is_tainted_expr(arg) <span style="color:#66d9ef">for</span> arg <span style="color:#f92672">in</span> node<span style="color:#f92672">.</span>args)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> isinstance(node, ast<span style="color:#f92672">.</span>Name):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> node<span style="color:#f92672">.</span>id <span style="color:#f92672">in</span> tainted
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> isinstance(node, ast<span style="color:#f92672">.</span>BinOp):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> is_tainted_expr(node<span style="color:#f92672">.</span>left) <span style="color:#f92672">or</span> is_tainted_expr(node<span style="color:#f92672">.</span>right)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">False</span></span></span></code></pre>
</figure><p>Because it&rsquo;s recursive, it doesn&rsquo;t need a special case for depth. Something like eval(str(input())) isn&rsquo;t handled by any explicit rule for &ldquo;calls wrapped in calls,&rdquo; it just falls out of the function calling itself: is_tainted_expr on the outer call checks its argument, which is another call, so it recurses, finds input(), and the True bubbles all the way back up.</p>
<h3 id="5-catching-the-sink">5. Catching the sink</h3>
<p>With the tainted set and is_tainted_expr in place, catching a sink is almost anticlimactic. Every time the walker hits a Call node, it checks the function name against SINKS, and if it matches, checks whether any argument is tainted:</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">visit_Call</span>(self, node):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> call_name(node) <span style="color:#f92672">in</span> SINKS:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> any(is_tainted_expr(arg) <span style="color:#66d9ef">for</span> arg <span style="color:#f92672">in</span> node<span style="color:#f92672">.</span>args):
</span></span><span style="display:flex;"><span>            print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Tainted data reaches sink at line </span><span style="color:#e6db74">{</span>node<span style="color:#f92672">.</span>lineno<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)</span></span></code></pre>
</figure><p>That&rsquo;s the whole loop: mark at the source, track through assignment, check at the sink. Everything else in the analyser is really about deciding what counts as &ldquo;tracking&rdquo; when the code gets less straightforward than a single assignment, which is where the harder decisions come in.</p>
<h2 id="the-decisions-that-actually-mattered">The decisions that actually mattered</h2>
<h3 id="1-ifelse-taking-the-union-of-both-branches">1. if/else: taking the union of both branches</h3>
<p>The tainted set works fine as long as code runs top to bottom in a straight line. The moment you hit an if, that stops being true, because now there are two possible futures, and by the time you reach the code after the if, you don&rsquo;t actually know which one happened.</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> something:
</span></span><span style="display:flex;"><span>    x <span style="color:#f92672">=</span> input()
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>    x <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;safe&#34;</span>
</span></span><span style="display:flex;"><span>print(x)</span></span></code></pre>
</figure><p>Is x tainted at that print? Depends entirely on which branch ran, and a static analyser never runs the code, so it can&rsquo;t know. I had to pick a rule anyway, and the rule I picked was: assume the worst. Walk both branches, and whatever ends up tainted in either one stays tainted after the if. In the example above, x is tainted in the if branch and clean in the else, so after merging, x is tainted, and the analyser flags the print.</p>
<p>That&rsquo;s technically wrong here, since if the else branch is the one that actually ran, x was never dangerous. But wrong in that specific direction is the safer kind of wrong. I&rsquo;d rather the analyser nag me about something that turns out to be fine than stay quiet about something that turns out not to be. Getting this backward, dropping a warning because one branch happened to be clean, is how a real taint analyser misses a real bug.</p>
<h3 id="2-function-calls-not-looking-inside-them">2. Function calls: not looking inside them</h3>
<p>The other decision was about what happens when tainted data gets passed into a function. The honest answer is: nothing happens. I don&rsquo;t look inside the function at all.</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">wrap</span>(val):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> val
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>x <span style="color:#f92672">=</span> input()
</span></span><span style="display:flex;"><span>y <span style="color:#f92672">=</span> wrap(x)
</span></span><span style="display:flex;"><span>eval(y)</span></span></code></pre>
</figure><p>A proper analyser would follow x into wrap, see that val comes back out unchanged as the return value, and figure out that y is tainted too. Mine doesn&rsquo;t do any of that. It treats every function call as a black box, and a black box that touched tainted data stays tainted going forward, no matter what actually happens inside.</p>
<p>This was less a design decision and more a line I drew to keep the project finishable. Actually tracking taint through a function call means tracking it across the call boundary, into the function&rsquo;s own scope, through its own returns, back out again, and doing that for functions that might be defined anywhere in the file, called in any order, maybe even recursively. That&rsquo;s a real feature, not an afternoon&rsquo;s work, and it was outside what I set out to build. So the rule became: taint in, taint out, no exceptions, and I didn&rsquo;t try to reason about what happens in between.</p>
<h2 id="where-its-wrong-on-purpose">Where it&rsquo;s wrong on purpose</h2>
<h3 id="1-false-positive-the-sanitiser-that-isnt">1. False positive: the sanitiser that isn&rsquo;t</h3>
<p>Once the analyser was working, I went looking for ways to break it, and the first one I found came from something that looks like good practice:</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span>x <span style="color:#f92672">=</span> input()
</span></span><span style="display:flex;"><span>x <span style="color:#f92672">=</span> sanitize(x)
</span></span><span style="display:flex;"><span>eval(x)</span></span></code></pre>
</figure><p>To a person reading this, sanitize is clearly meant to make x safe again, and if it actually does its job, the eval on the next line is fine. But the analyser has no idea what sanitize does. It&rsquo;s a function call, and per the rule from the section above, taint in, taint out, no exceptions. x goes in tainted, so x comes out tainted, and the analyser flags this exactly the same way it would flag eval(input()) directly.</p>
<p>This is a false positive, and it&rsquo;s a direct consequence of the black-box decision, not a separate bug. The analyser isn&rsquo;t wrong about what it can see; it genuinely cannot tell the difference between a function that sanitises its input and one that does nothing at all, or one that makes things worse. Fixing this properly means the analyser would need to know, ahead of time, which functions are sanitisers, which is its own can of worms since that list would have to be hand-maintained and would immediately be out of date the moment someone wrote a new one.</p>
<h3 id="2-false-negative-the-loop-that-never-runs">2. False negative: the loop that never runs</h3>
<p>The second one is worse, because it&rsquo;s not the analyser being overly cautious; it&rsquo;s the analyser missing something real.</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span>x <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;safe&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> i <span style="color:#f92672">in</span> range(<span style="color:#ae81ff">0</span>):
</span></span><span style="display:flex;"><span>    x <span style="color:#f92672">=</span> input()
</span></span><span style="display:flex;"><span>eval(x)</span></span></code></pre>
</figure><p>Here, the loop body never executes, so x really is safe by the time it reaches eval. But I don&rsquo;t handle loops at all in this analyser; loops were one of the things I explicitly cut to keep the scope small. So this snippet either gets skipped entirely or handled in a way that has nothing to do with what the loop would actually do at runtime, and either way, the analyser isn&rsquo;t reasoning about it correctly.</p>
<p>The reason I&rsquo;d call this the worst of the two problems is what each one costs you. A false positive wastes your time: you get flagged on something safe, you check it, you move on. A false negative costs you nothing, right up until the moment it costs you everything, because the whole point of a taint analyser is to be the thing that catches what you missed, and a tool that stays quiet about a real problem is worse than no tool at all, since it gives you false confidence instead of no confidence.</p>
<h2 id="what-i-got-out-of-it">What I got out of it</h2>
<p>Working on this project was my introduction to static analysis. Actually building it taught me that protecting a system doesn&rsquo;t always mean catching a mistake the moment you write it, sometimes it means going back over code that already exists and catching the problem proactively, before it&rsquo;s ever run against something that matters. A natural next step from here is dynamic analysis, which looks at the states of a program as it actually runs rather than reasoning about it ahead of time. There&rsquo;s a whole space of program-analysis techniques past this one too, mutation testing being one I ran into while researching this. I hope you found this as interesting as I did.</p>
<h2 id="further-readsreferences">Further reads/references</h2>
<ul>
<li><a href="https://www.qt.io/software-insights/taint-analysis-key-concepts-explained">Taint Analysis: Key Concepts Explained</a></li>
<li><a href="https://vfunction.com/blog/static-vs-dynamic-code-analysis/">Static vs Dynamic Code Analysis</a></li>
<li><a href="https://docs.python.org/3/library/ast.html">Python ast documentation</a></li>
</ul>
]]></content:encoded></item><item><title>Building a JSON Parser [with Python]</title><link>https://samuelomilo.com/posts/building-json-parser/</link><pubDate>Sun, 15 Sep 2024 17:36:58 +0100</pubDate><guid>https://samuelomilo.com/posts/building-json-parser/</guid><description>In this post, I will detail the steps I followed in building a JSON parser well enough for anyone with some programming knowledge and willingness to know what a parser and lexer are to follow along.
This JSON parser is built as part of the coding challenges by John Cricket and this particular attempt at the challenge is probably not the best out there. I expect there are better ways to do several things, but the primary aim for me was to have fun building 😄</description><content:encoded><![CDATA[<p>In this post, I will detail the steps I followed in building a JSON parser well enough for anyone with some programming knowledge and willingness to know what a <code>parser</code> and <code>lexer</code> are to follow along.</p>
<p>This JSON parser is built as part of the coding challenges by John Cricket and this particular attempt at the challenge is probably not the best out there. I expect there are better ways to do several things, but the primary aim for me was to have fun building 😄</p>
<h3 id="step-0---grammar-as-the-grand-start">STEP 0 - Grammar as the grand start</h3>
<p>Grammar, in the theory of computation, refers to a set of rules that define acceptable strings in a language. Quite similar to grammar in the English language if you ask me. Think of how you know “I can sing” is a correct sentence but “I sing can” isn’t.</p>
<p>The role of the parser—in a compilation process—is to validate syntactic structure. It accepts a sequence of tokens—to be discussed next—and checks that the sequence is valid according to the language&rsquo;s grammar.</p>
<p>Right before the parser gets into action, there is the lexer. The lexer, also usually a part of the compiler, is responsible for reading the text you write and creating meaningful tokens of it. A token is the simplest meaningful unit in a language. Take a look at the examples <a href="https://en.wikipedia.org/wiki/Lexical_analysis#Lexical_token_and_lexical_tokenization">here</a> to better understand what tokens are.</p>
<p>Back to the grammar; for the parser to know what sequence of tokens is valid, we clearly define the acceptable sequence i.e. the grammar. As a similar reference, in the English language modal verbs, such as “can”, always come before the main verb, such as “sing” hence why the sentence “I can sing” is valid and the other isn’t.</p>
<p>I came up with the following grammar for JSON. The grammar references <a href="http://json.orghttps://www.json.org/json-en.html">json.org</a> for notation and structure.</p>
<p><img loading="lazy" src="/posts/building-json-parser/json-grammar.png" type="" alt="JSON grammar"  /></p>
<p>The notation above may be strange if you’re unfamiliar with the topic; It is a common way of representing grammar using production rules—each line with an arrow. This particular set of production rules is interpreted as the following from top to bottom:</p>
<ul>
<li>An object (O), identified with an open brace, a list of members(N), followed by the closed brace. A member refers to a key-value pair</li>
<li>A list of members (N) represented either by an empty string, a member(M), or a member(M) followed by a comma, followed by a list of members (N)</li>
<li>A member(M) is identified as a string followed by a colon(:) followed by a value (V)</li>
<li>A value(V) identified as either an object (O), an array (A), a boolean (b), null (n), or a string(s)</li>
<li>An array(A) is identified as a left bracket followed by a list of elements(L) followed by a right bracket</li>
<li>A list of elements identified as a single value, a value followed by a comma, followed by another list of elements of an empty string</li>
</ul>
<h3 id="step-11-lexer-but-just-open-and-close">STEP 1.1 Lexer, but just open and close</h3>
<p>For this step, I built a lexer that recognizes the <code>{</code> and <code>}</code> characters as tokens. To begin with the lexer, a <code>Lexer</code> class:</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> __init__(self, input: str):
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>position <span style="color:#f92672">=</span> <span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>input <span style="color:#f92672">=</span> input
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>line_number <span style="color:#f92672">=</span> <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">readCharacter</span>(self) <span style="color:#f92672">-&gt;</span> <span style="color:#66d9ef">None</span> <span style="color:#f92672">|</span> str:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>position <span style="color:#f92672">&gt;=</span> len(self<span style="color:#f92672">.</span>input):
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">None</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        character <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>input[self<span style="color:#f92672">.</span>position]
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>position <span style="color:#f92672">+=</span> <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> character</span></span></code></pre>
</figure><!-- raw HTML omitted -->
<p>The class has a <code>readCharacter</code> method, which returns the character to the current <code>position</code> on every call and increments the position. The idea is to use the position as a pointer and iterate through every character in the text input. I hope to use the <code>line_number</code>attribute for error reporting 🤞</p>
<p>To recognize the different types of tokens, I use a <code>Token</code> class with a type attribute and a literal for actually storing the string/character:</p>

<figure class="highlight">
  <figcaption>
    <span>token.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> enum <span style="color:#f92672">import</span> Enum
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">TokenTypes</span>(Enum):
</span></span><span style="display:flex;"><span>    LEFT_BRACE <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;left_brace&#34;</span> <span style="color:#75715e"># {</span>
</span></span><span style="display:flex;"><span>    RIGTH_BRACE <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;right_brace&#34;</span> <span style="color:#75715e"># }</span>
</span></span><span style="display:flex;"><span>    EOF <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;eof&#34;</span> <span style="color:#75715e"># mark end of file</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Token</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> __init__(self, literal: str, type: TokenTypes):
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>literal <span style="color:#f92672">=</span> literal
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>type <span style="color:#f92672">=</span> type
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> __repr__(self) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Token[literal: &#39;</span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;, type: </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>type<span style="color:#f92672">.</span>name<span style="color:#e6db74">}</span><span style="color:#e6db74">]&#34;</span></span></span></code></pre>
</figure><p>The only types known for now are the left brace, the right brace, and an EOF type I use to recognize when we have read all of the text.</p>
<p>The parser mostly needs the lexer to produce the next token in the sequence so I make a method in the Lexer class for this; every time it’s called, the lexer returns the next token in the sequence</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">nextToken</span>(self):
</span></span><span style="display:flex;"><span>        character <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>readCharacter()
</span></span><span style="display:flex;"><span>        token <span style="color:#f92672">=</span> <span style="color:#66d9ef">None</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;{&#34;</span>
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>LEFT_BRACE)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;}&#34;</span>:
</span></span><span style="display:flex;"><span>             token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>RIGHT_BRACE)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character <span style="color:#f92672">==</span> <span style="color:#66d9ef">None</span>:
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(<span style="color:#e6db74">&#34;&#34;</span>, TokenTypes<span style="color:#f92672">.</span>EOF)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">Exception</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;character at position </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>position <span style="color:#f92672">-</span> <span style="color:#ae81ff">1</span><span style="color:#e6db74">}</span><span style="color:#e6db74"> not recognised&#39;</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> token</span></span></code></pre>
</figure><blockquote>
<p>Notes on the Exceptions using <code>self.position</code></p>
<p>The idea of raising errors with messages having the position was to replicate error messages that point you to the particular position on a line where the error occurred. This worked well with single line text but to get it accurate for multiple lines would require work I am yet to get to as of now</p>
</blockquote>
<p>That’s all for the basic lexer 🤩 . Way easier than the earlier explanation of what a lexer is right?</p>
<p>I wrote some unit tests to test things out but I’ll leave that out of this post. The final set of tests can be found in the <a href="https://github.com/Aptcoder/coding-challenges/tree/main/json-parser">GitHub repo</a>.</p>
<h3 id="step-12-parser-but-just-open-and-close">STEP 1.2 Parser, but just open and close</h3>
<p>Next comes building the parser capable of parsing an empty object <code>{}</code>. As with the Lexer, I use a Parser class for everything related:</p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> __init__(self, input: str) <span style="color:#f92672">-&gt;</span> <span style="color:#66d9ef">None</span>:
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>lexer <span style="color:#f92672">=</span> Lexer(input)
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>currentToken <span style="color:#f92672">=</span> <span style="color:#66d9ef">None</span> <span style="color:#75715e"># Current token to parse</span>
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>peekToken <span style="color:#f92672">=</span> <span style="color:#66d9ef">None</span> <span style="color:#75715e"># Next token that will be parsed. Knowing this will be useful for selecting valid rules.</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">nextToken</span>(self):
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>currentToken <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>peekToken
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>peekToken <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>lexer<span style="color:#f92672">.</span>nextToken()</span></span></code></pre>
</figure><p>The class uses an instance of the lexer class for getting tokens — from the input text. The <code>nextToken</code> is used for setting the <code>currentToken</code> attribute to the next token to be evaluated. A <code>peekToken</code> attribute also exists in the class for the token right after the current token. This allows the parser to take a “peek” ahead.</p>
<p>To parse the JSON, I explored the concept of a recursive descent parser. More about that <a href="https://en.wikipedia.org/wiki/Recursive_descent_parser">here</a>. Parsers are split into top-down parsers and bottom-up parsers. Recursive descent parsing is an approach to top-down parsing I found fairly easy to implement. The idea — of recursive descent parsing — as I came to understand it is to have a procedure/function representing each non-terminal in my grammar — the big letters in the above grammar. Each of these functions will attempt to parse the following tokens according to the right-hand side of the production rule. If another(or the same) non-terminal is part of the production rule, the function for this non-terminal is called, hence the recursive part of the name.</p>
<p>Getting into actual coding; First I need a function that actually starts the parsing</p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseProgram</span>(self) <span style="color:#f92672">-&gt;</span> ProgramNode:
</span></span><span style="display:flex;"><span>        <span style="color:#75715e"># Call next token twice to allow currentToken and peekToken to be properly initialised</span>
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>        program_node <span style="color:#f92672">=</span> ProgramNode()
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>LEFT_BRACE:
</span></span><span style="display:flex;"><span>            parent_object: ObjectNode <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>parseObject()
</span></span><span style="display:flex;"><span>            program_node<span style="color:#f92672">.</span>parent_object <span style="color:#f92672">=</span> parent_object
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Invalid token </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> program_node</span></span></code></pre>
</figure><p>The <code>parseProgram</code> method is to be called to begin the process. It calls <code>self.nextToken</code> twice to initialize the <code>currentToken</code> and <code>peekToken</code> to actual tokens. JSON — an object notation — is a bunch of members wrapped in an object so the first token to expect in a valid JSON should be the start of an object, <code>{</code> — the left brace.</p>
<p>If the <code>currentToken</code> is a left brace, then it’s fair to assume we’re dealing with an object and try to parse the object by calling the <code>parseObject</code> method shown below:</p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseObject</span>(self):
</span></span><span style="display:flex;"><span>        object_node <span style="color:#f92672">=</span> ObjectNode()
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">!=</span> TokenTypes<span style="color:#f92672">.</span>RIGTH_BRACE:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">pass</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Invalid token </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> object_node</span></span></code></pre>
</figure><p>In this method, I check that the next token is a right brace. And that’s all, the parser now recognizes the JSON <code>{}</code></p>
<blockquote>
<p>Nodes - Note on the node objects seen so far</p>
<p>The ObjectNode and ProgramNode classes seen so far refer to nodes of an <a href="https://en.wikipedia.org/wiki/Abstract_syntax_tree">Abstract Syntax Tree(AST).</a> The parser as part of the compilation process is responsible for generating an abstract syntax tree to be utilized by the following stages of the process. In the code so far, I have created these nodes but I have chosen not to discuss the nodes or the AST to leave the post less complex.</p>
</blockquote>
<h3 id="step-21-more-strings-more-power-to-the-lexer">STEP 2.1 More strings, more power to the Lexer</h3>
<p>To allow the Lexer to recognize strings both for member names and member values, I add a string token type to the set of known types</p>

<figure class="highlight">
  <figcaption>
    <span>token.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">TokenTypes</span>(Enum):
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    STRING <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;string&#34;</span> <span style="color:#75715e"># a string</span></span></span></code></pre>
</figure><p>In the lexer’s <code>nextToken</code> method, we recognize strings as a sequence of characters that start and end with a quotation mark.</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">nextToken</span>(self):
</span></span><span style="display:flex;"><span>        character <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>readCharacter()
</span></span><span style="display:flex;"><span>        token <span style="color:#f92672">=</span> <span style="color:#66d9ef">None</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;{&#34;</span>:
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>LEFT_BRACE)
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#39;&#34;&#39;</span>:
</span></span><span style="display:flex;"><span>            name <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>readString()
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(name, TokenTypes<span style="color:#f92672">.</span>STRING)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">Exception</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;character at position </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>position <span style="color:#f92672">-</span> <span style="color:#ae81ff">1</span><span style="color:#e6db74">}</span><span style="color:#e6db74"> not recognised&#39;</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> token</span></span></code></pre>
</figure><p>When the currently read character is <code>“</code> the set of characters that follow should make up a string, and I use the <code>readString</code> method to read these characters into one Python string.</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">readString</span>(self):
</span></span><span style="display:flex;"><span>        string <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;&#34;</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> char <span style="color:#f92672">:=</span> self<span style="color:#f92672">.</span>readCharacter():
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> char <span style="color:#f92672">==</span> <span style="color:#e6db74">&#39;&#34;&#39;</span>:
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">break</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                string <span style="color:#f92672">=</span> string <span style="color:#f92672">+</span> char
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> string</span></span></code></pre>
</figure><p>The <code>nextToken</code> method uses the string literal returned in creating a token and returns that token.
While we’re on strings; the lexer should also recognize commas and colons. The token types are added as that for strings was:</p>

<figure class="highlight">
  <figcaption>
    <span>token.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">TokenTypes</span>(Enum):
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    STRING <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;string&#34;</span> <span style="color:#75715e"># a string</span>
</span></span><span style="display:flex;"><span>    COLON <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;:&#34;</span> <span style="color:#75715e"># colon token type</span>
</span></span><span style="display:flex;"><span>    COMMA <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;comma&#34;</span> <span style="color:#75715e"># comma `,`</span></span></span></code></pre>
</figure><p>The <code>nextToken</code> can easily recognize these characters so the tokens are easily created</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">nextToken</span>(self):
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#39;:&#39;</span>:
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>COLON)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;,&#34;</span>:
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>COMMA)</span></span></code></pre>
</figure><h3 id="step-22-what-about-spaces">STEP 2.2 What about spaces</h3>
<p>So far the lexer has iterated through the text assuming every character is a recognized one or it throws an error. But in writing JSON <code>{&quot;hey&quot;:&quot;there&quot;}</code> is just as valid as <code>{ &quot;hey&quot;:      &quot;there&quot;}</code>. The space between tokens should be ignored. The lexer currently will throw an error whenever it meets a space character. I fix this by adding every space character as a recognized one but not returning as a token, instead, keep reading the next characters until we get to read a valid token</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">nextToken</span>(self):
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character<span style="color:#f92672">.</span>isspace():
</span></span><span style="display:flex;"><span>            <span style="color:#75715e"># If character is space, skip it</span>
</span></span><span style="display:flex;"><span>            self<span style="color:#f92672">.</span>skipSpaces()
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">return</span> self<span style="color:#f92672">.</span>nextToken()</span></span></code></pre>
</figure><p>The skip spaces method keeps reading characters until we get to a non-space character, then we call nextToken again so we get the next viable token</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">skipSpaces</span>(self):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> char <span style="color:#f92672">:=</span> self<span style="color:#f92672">.</span>readCharacter():
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> char<span style="color:#f92672">.</span>isspace():
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">if</span> char <span style="color:#f92672">==</span> <span style="color:#e6db74">&#39;</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#39;</span> <span style="color:#f92672">or</span> char <span style="color:#f92672">==</span> <span style="color:#e6db74">&#39;</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">&#39;</span>:
</span></span><span style="display:flex;"><span>                    self<span style="color:#f92672">.</span>line_number <span style="color:#f92672">+=</span> <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">continue</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                self<span style="color:#f92672">.</span>position <span style="color:#f92672">-=</span> <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">break</span></span></span></code></pre>
</figure><p>Skipping the spaces is a good enough solution for handling spaces for JSON, In a programming language where space is actually a valid/required part of the syntax the space may actually be kept as a token — I think?</p>
<h3 id="step-23-parsers-first-steps">STEP 2.3 Parser’s first steps</h3>
<p>To take some baby steps, the parser should be able to parse <code>{&quot;name&quot;: &quot;samuel&quot;}</code> — Object, with a single member whose key and value are strings. The lexer already recognizes string tokens and the semi-colon token so I only need the Object to call the procedure to parse a member. Remember I said a procedure/method will exist for each non-terminal.</p>
<p>To add this, I updated the <code>parseObject</code> procedure as such:</p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseObject</span>(self):
</span></span><span style="display:flex;"><span>        object_node <span style="color:#f92672">=</span> ObjectNode()
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">!=</span> TokenTypes<span style="color:#f92672">.</span>RIGTH_BRACE:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>STRING:
</span></span><span style="display:flex;"><span>                member <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>parseMember()
</span></span><span style="display:flex;"><span>                object_node<span style="color:#f92672">.</span>members<span style="color:#f92672">.</span>append(member)
</span></span><span style="display:flex;"><span>                self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Invalid token </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> object_node</span></span></code></pre>
</figure><p>The change adds a check to see if the current token is a string type. If it is, it’s okay to assume we’re now dealing with a member (a key-value pair), so I call the function to parse a member.
The function to parse a member checks that the next token after the string (the key) is a colon, otherwise it throws an error and skips the colon to the next token, then checks that this current token is also a string(the value).</p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseMember</span>(self) <span style="color:#f92672">-&gt;</span> MemberNode:
</span></span><span style="display:flex;"><span>        member_node <span style="color:#f92672">=</span> MemberNode()
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>peekToken<span style="color:#f92672">.</span>type <span style="color:#f92672">!=</span> TokenTypes<span style="color:#f92672">.</span>COLON:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">&#39;Colon required in defining a member&#39;</span>)
</span></span><span style="display:flex;"><span>        member_node<span style="color:#f92672">.</span>key <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>currentToken
</span></span><span style="display:flex;"><span>        <span style="color:#75715e"># Skip the colon and go to the next token</span>
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">!=</span> TokenTypes<span style="color:#f92672">.</span>STRING:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">&#34;A member requires a string value&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> member_node</span></span></code></pre>
</figure><p>With these changes, the parse can now parse the JSON <code>{&quot;name&quot;: &quot;samuel&quot;}</code>.</p>
<h3 id="step-24-enough-steps-to-walk">STEP 2.4 Enough steps to walk</h3>
<p>For the next step, I update the parser to recognize JSON with multiple key-value pairs like <code>{&quot;name&quot;: &quot;samuel&quot;, &quot;random&quot;: &quot;yes&quot;}</code>. To do this the parser needs to know to expect a comma after every member except the last one.</p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>	<span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>	<span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseObject</span>(self):
</span></span><span style="display:flex;"><span>			<span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">!=</span> TokenTypes<span style="color:#f92672">.</span>RIGHT_BRACE:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>STRING:
</span></span><span style="display:flex;"><span>                member <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>parseMember()
</span></span><span style="display:flex;"><span>                object_node<span style="color:#f92672">.</span>members<span style="color:#f92672">.</span>append(member)
</span></span><span style="display:flex;"><span>                self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>COMMA: <span style="color:#75715e"># If the current token is a comma</span>
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>peekToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>STRING: <span style="color:#75715e"># If the next token is a string, then there are more members to follow</span>
</span></span><span style="display:flex;"><span>                        self<span style="color:#f92672">.</span>nextToken() <span style="color:#75715e"># Skip the comma</span>
</span></span><span style="display:flex;"><span>                    <span style="color:#75715e"># Otherwise do nothing</span>
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">else</span>: <span style="color:#75715e"># If the current token is not a comma, then that better be the end</span>
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">!=</span> TokenTypes<span style="color:#f92672">.</span>RIGHT_BRACE:
</span></span><span style="display:flex;"><span>                        <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Comma expected before next member&#39;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Invalid token </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> object_node</span></span></code></pre>
</figure><p>With this change, the parser after parsing a member checks if the next token is a comma, if it is, then it skips the comma for the loop to parse the next member in the next iteration. If it isn’t a comma, then it’s expected to be a right brace.</p>
<p>That’s it! the parser can now parse JSON with all its values as strings.</p>
<h3 id="step-31-a-bigger-lexer">STEP 3.1 A bigger lexer</h3>
<p>To improve the lexer&rsquo;s capability, I updated it to recognize numerics, booleans, null, left bracket, and right bracket as tokens.</p>
<p>As usual, I added the new token types;</p>

<figure class="highlight">
  <figcaption>
    <span>token.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">TokenTypes</span>(Enum):
</span></span><span style="display:flex;"><span>	<span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    LEFT_BRACKET <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;left_bracket&#34;</span> <span style="color:#75715e"># [</span>
</span></span><span style="display:flex;"><span>    RIGHT_BRACKET <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;right_bracket&#34;</span> <span style="color:#75715e"># ]</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    NUMERIC <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;numeric&#34;</span>
</span></span><span style="display:flex;"><span>    BOOLEAN_TRUE <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;true&#34;</span> <span style="color:#75715e"># true</span>
</span></span><span style="display:flex;"><span>    BOOLEAN_FALSE <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;false&#34;</span>
</span></span><span style="display:flex;"><span>    NULL <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;null&#34;</span></span></span></code></pre>
</figure><p>With the token types added, the lexer can be updated to identify the relevant tokens. Identifying the left and right brackets is easier to do:</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">nextToken</span>(self):
</span></span><span style="display:flex;"><span>    character <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>readCharacter()
</span></span><span style="display:flex;"><span>    token <span style="color:#f92672">=</span> <span style="color:#66d9ef">None</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;{&#34;</span>:
</span></span><span style="display:flex;"><span>        token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>LEFT_BRACE)
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>tokens<span style="color:#f92672">.</span>append(token)
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">elif</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;]&#34;</span>:
</span></span><span style="display:flex;"><span>        token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>RIGHT_BRACKET)
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>tokens<span style="color:#f92672">.</span>append(token)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">elif</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;[&#34;</span>:
</span></span><span style="display:flex;"><span>        token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>LEFT_BRACKET)
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>tokens<span style="color:#f92672">.</span>append(token)
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">Exception</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;character at position </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>position <span style="color:#f92672">-</span> <span style="color:#ae81ff">1</span><span style="color:#e6db74">}</span><span style="color:#e6db74"> not recognised&#39;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> token</span></span></code></pre>
</figure><p>Identifying booleans, null, and numeric required extra functions for actually reading the next sequence of characters. A <code>readNumeric</code> method in the lexer for reading numerics :[</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">readNumeric</span>(self):
</span></span><span style="display:flex;"><span>        numeric <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>input[self<span style="color:#f92672">.</span>position <span style="color:#f92672">-</span> <span style="color:#ae81ff">1</span>]
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> char <span style="color:#f92672">:=</span> self<span style="color:#f92672">.</span>readCharacter():
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> <span style="color:#f92672">not</span> char<span style="color:#f92672">.</span>isnumeric():
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">break</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                numeric <span style="color:#f92672">=</span> numeric <span style="color:#f92672">+</span> char
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>position <span style="color:#f92672">-=</span> <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> numeric</span></span></code></pre>
</figure><p>The method reads a new character until it gets to one that isn’t numeric. Right before it returns I adjust the position attribute one step backward so the next call to <code>nextToken</code> reads the right character.
Also, a <code>readKeyword</code> method in which I read booleans(true or false) and the null type.</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span>keywords <span style="color:#f92672">=</span> {<span style="color:#e6db74">&#34;true&#34;</span>: TokenTypes<span style="color:#f92672">.</span>BOOLEAN_TRUE, <span style="color:#e6db74">&#34;false&#34;</span>: TokenTypes<span style="color:#f92672">.</span>BOOLEAN_FALSE, <span style="color:#e6db74">&#34;null&#34;</span>: TokenTypes<span style="color:#f92672">.</span>NULL}
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#39;&#39;&#39;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    This checks if a set of characters is either a boolean value or null
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#39;&#39;&#39;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">readKeyword</span>(self):
</span></span><span style="display:flex;"><span>        start_position <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>position <span style="color:#f92672">-</span> <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>        keyword <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>input[start_position]
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> char <span style="color:#f92672">:=</span> self<span style="color:#f92672">.</span>readCharacter():
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> <span style="color:#f92672">not</span> char<span style="color:#f92672">.</span>isalpha():
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">break</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                keyword <span style="color:#f92672">=</span> keyword <span style="color:#f92672">+</span> char
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> keyword <span style="color:#f92672">in</span> keywords<span style="color:#f92672">.</span>keys():
</span></span><span style="display:flex;"><span>            token_type <span style="color:#f92672">=</span> keywords[keyword]
</span></span><span style="display:flex;"><span>            self<span style="color:#f92672">.</span>position <span style="color:#f92672">-=</span> <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">return</span> keyword, token_type
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">Exception</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;character at position </span><span style="color:#e6db74">{</span>start_position<span style="color:#e6db74">}</span><span style="color:#e6db74"> not recognised&#39;</span>)</span></span></code></pre>
</figure><p>This method is similar to <code>readString</code> but with the addition of the if condition which checks if the string we have so far is in the known list of keywords. There’s also the important difference with when the functions are called. The <code>readString</code> is called when we get to a quote character — identifies the beginning of a string — while the <code>readKeyword</code> is called when we get to an alphabet character.</p>
<p>with that sorted, the lexer can now read and identify those token types:</p>

<figure class="highlight">
  <figcaption>
    <span>lexer.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Lexer</span>:
</span></span><span style="display:flex;"><span>	<span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>	<span style="color:#66d9ef">def</span> <span style="color:#a6e22e">nextToken</span>(self):
</span></span><span style="display:flex;"><span>        character <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>readCharacter()
</span></span><span style="display:flex;"><span>        token <span style="color:#f92672">=</span> <span style="color:#66d9ef">None</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> character <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;{&#34;</span>:
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(character, TokenTypes<span style="color:#f92672">.</span>LEFT_BRACE)
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character<span style="color:#f92672">.</span>isnumeric():
</span></span><span style="display:flex;"><span>            numeric <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>readNumeric()
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(numeric, TokenTypes<span style="color:#f92672">.</span>NUMERIC)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> character<span style="color:#f92672">.</span>isalpha():
</span></span><span style="display:flex;"><span>            keyword, token_type <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>readKeyword()
</span></span><span style="display:flex;"><span>            token <span style="color:#f92672">=</span> Token(keyword, token_type)
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> token</span></span></code></pre>
</figure><h3 id="step-32-a-bigger-parser">STEP 3.2 A bigger parser</h3>
<p>With the lexer able to identify all possible tokens, the parser can be updated to parse all the possible types for a value.</p>
<p>I modified the procedure for parsing a member; instead of just checking that the value is a string the parser should attempt to parse the value using the procedure <code>parseValue</code></p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>	<span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseMember</span>(self) <span style="color:#f92672">-&gt;</span> MemberNode:
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>        member_node<span style="color:#f92672">.</span>value <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>parseValue()
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> member_node
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>	<span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseValue</span>(self) <span style="color:#f92672">-&gt;</span> Token:
</span></span><span style="display:flex;"><span>        valueNode <span style="color:#f92672">=</span> ValueNode()
</span></span><span style="display:flex;"><span>        accepted_value_token_types <span style="color:#f92672">=</span> [TokenTypes<span style="color:#f92672">.</span>STRING, TokenTypes<span style="color:#f92672">.</span>NUMERIC, TokenTypes<span style="color:#f92672">.</span>BOOLEAN_FALSE, TokenTypes<span style="color:#f92672">.</span>BOOLEAN_TRUE, TokenTypes<span style="color:#f92672">.</span>NULL]
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">in</span> accepted_value_token_types:
</span></span><span style="display:flex;"><span>            valueNode<span style="color:#f92672">.</span>value <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>currentToken
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>LEFT_BRACKET:
</span></span><span style="display:flex;"><span>            arrayNode <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>parseArray()
</span></span><span style="display:flex;"><span>            valueNode<span style="color:#f92672">.</span>value <span style="color:#f92672">=</span> arrayNode
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">elif</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>LEFT_BRACE:
</span></span><span style="display:flex;"><span>            objectNode <span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>parseObject()
</span></span><span style="display:flex;"><span>            valueNode<span style="color:#f92672">.</span>value <span style="color:#f92672">=</span> objectNode
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">SyntaxError</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Invalid token for value: </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> valueNode</span></span></code></pre>
</figure><p>The procedure checks that the current token type is in one of the accepted value types. You’ll also notice another procedure for parsing arrays. If the current token type is a left bracket then we should be at the beginning of an array so the function <code>parseArray</code> is called</p>

<figure class="highlight">
  <figcaption>
    <span>parser.py</span>
  </figcaption>
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Parser</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>	<span style="color:#66d9ef">def</span> <span style="color:#a6e22e">parseArray</span>(self):
</span></span><span style="display:flex;"><span>        arrayNode <span style="color:#f92672">=</span> ArrayNode()
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>        accepted_value_token_types <span style="color:#f92672">=</span> [TokenTypes<span style="color:#f92672">.</span>STRING, TokenTypes<span style="color:#f92672">.</span>NUMERIC, TokenTypes<span style="color:#f92672">.</span>BOOLEAN_FALSE, TokenTypes<span style="color:#f92672">.</span>BOOLEAN_TRUE, TokenTypes<span style="color:#f92672">.</span>NULL]
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">!=</span> TokenTypes<span style="color:#f92672">.</span>RIGHT_BRACKET:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">in</span> accepted_value_token_types:
</span></span><span style="display:flex;"><span>                arrayNode<span style="color:#f92672">.</span>elements<span style="color:#f92672">.</span>append(self<span style="color:#f92672">.</span>currentToken)
</span></span><span style="display:flex;"><span>                self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>COMMA:
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>peekToken<span style="color:#f92672">.</span>type <span style="color:#f92672">in</span> accepted_value_token_types:
</span></span><span style="display:flex;"><span>                        self<span style="color:#f92672">.</span>nextToken()
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                        <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">Exception</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Unrecognised array element </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>peekToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">if</span> self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> TokenTypes<span style="color:#f92672">.</span>RIGHT_BRACKET:
</span></span><span style="display:flex;"><span>                        <span style="color:#66d9ef">continue</span>
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                        <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">Exception</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Unrecognised array element </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">Exception</span>(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Unrecognised array element </span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>currentToken<span style="color:#f92672">.</span>literal<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> arrayNode</span></span></code></pre>
</figure><p>Parsing of an array generally involves checking that until we meet a right bracket that closes the array, we keep getting a sequence of acceptable tokens in an array always split by a comma.</p>
<p>In the <code>parseValue</code> method, you’ll also notice a <code>parseObject</code> call; this calls the same function we call when we begin to parse the program because remember, the first thing in JSON is an object.</p>
<p>That’s it. The parser is now capable of parsing JSON as complex as this:</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;key&#34;</span>: <span style="color:#e6db74">&#34;value&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;key-n&#34;</span>: <span style="color:#ae81ff">101</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;key-o&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;inner key&#34;</span>: <span style="color:#e6db74">&#34;inner value&#34;</span>
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;key-l&#34;</span>: [<span style="color:#e6db74">&#34;list value&#34;</span>]
</span></span><span style="display:flex;"><span>}</span></span></code></pre>
</figure><p>There are several tests for the parser in the codebase for automated tests but running this parser would look something like this:</p>

<figure class="highlight">
  <pre tabindex="0" ><code class="language-python" data-lang="python"><span style="display:flex;"><span>python json<span style="color:#f92672">-</span>parser <span style="color:#f92672">--</span>file test<span style="color:#f92672">.</span>json</span></span></code></pre>
</figure><p>The parser in this case would attempt to parse <code>test.json</code> and throw an error if the JSON in the file is invalid.</p>
<p>I had fun building and look forward to doing more similar projects. Until then, cheers 🥂</p>
]]></content:encoded></item></channel></rss>